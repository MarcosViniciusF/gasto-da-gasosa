import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Gasto da gasosa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
